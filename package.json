{
  "name": "mrmelo-vibes",
  "version": "1.0.0",
  "description": "Nostalgia player — stream your favourite decades",
  "scripts": {
    "build": "echo 'Static site — no build required'",
    "vercel-build": "echo 'Static site — no build required'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1"
  }
}
